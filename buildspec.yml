version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 22
    commands:
      - pip install aws-sam-cli
  build:
    commands:
      - sam build
      - sam deploy --no-fail-on-empty-changeset

artifacts:
  files:
    - "**/*"
